export interface Car {
    product_url: string,
    source_url?: string,
    page_id: string,
    brand: string,
    brand_id?: string,
    code?: string,
    summary: string,
    last_crawled: string,
    description_ai?: string,
    make?: string,
    make_id?: string,
    make_ai?: string,
    model_ai?: string,
    release_date_ai?: string,
    release_date_approximate?: string,
    details: {
        Status: string,
        Scale: string,
        Marque: string,
        id: string
    },
    image_url: string | string[],
    images: {
        original_url: string,
        s3_url: string,
    }[],
    product_line?: string,
    product_line_id?: string,
    id: string,
    image_s3_key: string,
    image_s3_url: string,
    title: string,
    scale?: string,
    is_chase?: boolean,
    is_limited?: boolean,
    limited_pieces?: number,
    own?: boolean,
    liked?: boolean,
    original_id?: string,
    originalId?: string,
    loadingAdd?: boolean,
    loadingLike?: boolean,
}
